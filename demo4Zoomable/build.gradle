apply from:'https://raw.githubusercontent.com/lsjwzh/widget.gradle/master/app-androidx.gradle'

dependencies {
    configurations {
        all*.exclude group: 'com.android.support'
    }
    implementation project(":widget.pulltozoom")
    implementation project(':powerfulscrollview')
}